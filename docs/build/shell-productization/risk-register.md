# Risk register — Gosling shared shell productization

Status vocabulary: `open`, `mitigating`, `closed`, `accepted`. Review at every gate exit.

| ID          | Risk                                                                                                                                  | Likelihood | Impact   | Prevention / mitigation                                                                                                  | Trigger / contingency                                                                       | Owner / gate           | Status                                                                                                                                                                                                                                                                      |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SHP-RSK-001 | Reusing full Desktop `main.ts`/preload gives shells broad filesystem/settings/provider authority and defeats minimal-host intent      | medium     | critical | ADR-0008 mandates separate entries and frozen eight-operation IPC allowlist; negative-space tests remain required        | Any shell imports full preload/main IPC registry → stop Gate 4 and extract boundary         | Runtime, G2–5          | mitigating; separate entries, package readback, and negative-space tests pass                                                                                                                                                                                               |
| SHP-RSK-002 | Electron app identity is set after userData/single-instance/protocol initialization, causing cross-product collisions                 | medium     | critical | Resolve profile and set app identity before readiness/lock; app identity unit and coexistence tests                      | Shared path/lock/protocol observed → block Gate 6 and repair ordering                       | Runtime, G3–6          | mitigating; ordering/path tests pass, installed coexistence pending                                                                                                                                                                                                         |
| SHP-RSK-003 | Forge/workflow/platform scripts derive different names/IDs/assets from independent environment values                                 | high       | high     | Canonical profile resolution/hash; thin adapters; package readback; collision checks                                     | Any artifact field differs from manifest → artifact invalid, no upload                      | Build, G3/7            | mitigating; resolver/Forge/hash and macOS arm64 readback pass, other targets pending                                                                                                                                                                                        |
| SHP-RSK-004 | Product profile becomes a second runtime/config/credential authority                                                                  | low        | critical | ADR-0007 strict profile/provisioning split rejects secret/settings/provider/policy/domain values; Rust remains authority | Requested profile field encodes runtime secret/policy semantics → plan review and reject    | Architecture, G2–4     | closed for profile boundary; runtime authority proof continues Gate 4                                                                                                                                                                                                       |
| SHP-RSK-005 | Renderer can assert shell identity/namespace/provisioning through IPC/ACP parameters                                                  | low        | critical | No such preload operations; compare runtime metadata to fixed profile; server-fixed existing contract                    | Mismatch or renderer parameter surface → fail closed and regression test                    | Security, G4/6         | mitigating; IPC/preload/preflight/package negative-space tests pass                                                                                                                                                                                                         |
| SHP-RSK-006 | Unit/source tests pass while packaged binary/resource/CSP/preload path fails                                                          | high       | high     | Actual packaged E2E, resource verifier, tamper tests, launch/restart/cleanup evidence                                    | Packaged test unavailable on target → cannot mark target release-ready                      | QA, G6–8               | mitigating; macOS arm64 structure/preflight/cleanup and live unpackaged session restart observed; packaged matrix pending                                                                                                                                                   |
| SHP-RSK-007 | Child process leaks or stale process-registry entry survives startup/crash/quit                                                       | medium     | high     | One lifecycle owner; bounded graceful/forced cleanup; PID/registry tests and installed replay                            | Any orphan after test → gate blocker; retain diagnostics and patch root cause               | Runtime, G4/6          | mitigated locally after SHP-DEF-046/047: destroyed-renderer notification cannot abort cleanup, startup reconciles product-local stale records, and rebuilt packaged close leaves no process and an empty registry; exact-revision and broader failure matrix remain pending |
| SHP-RSK-008 | Diagnostics expose server secrets, credential values, prompts, paths, or unbounded logs                                               | medium     | critical | Allowlisted fields, sentinel redaction, byte ceilings, private atomic write, inspect-before-share warning                | Sentinel appears or bundle too broad → block release; rotate any exposed test secret        | Security, G4–8         | mitigating; focused redaction/bounds/private-write tests pass, packaged export pending                                                                                                                                                                                      |
| SHP-RSK-009 | Handoff silently widens authority or accepts forged destination/reference data                                                        | medium     | critical | Server-prepared versioned envelope; explicit confirmation; protocol allowlist; no renderer-built envelope                | Invalid destination/version/ref or no confirmation → deny and diagnose                      | Security, G4–6         | mitigating; sender plus focused receiver reject malformed envelopes and receiver creates only a no-auto-submit review draft; UI/installed proof pending                                                                                                                     |
| SHP-RSK-010 | Existing V8 helper/version/checksum mismatch leads to bypassing archive verification                                                  | low        | critical | Wrapper/build-script contract confirmed; immutable gzip checksum and fail-closed regression suite retained               | Missing checksum/asset → operator seed/build-from-source; never disable check               | CI/supply chain, G0–1  | closed; local matrix and two Linux helper/prepare executions pass (`evidence/r0.md`)                                                                                                                                                                                        |
| SHP-RSK-011 | Rust cache restores object metadata without native archive, recreating flaky `rusty_v8` failures                                      | low        | high     | Helper regression/fresh prepare runs before rust-cache; one runner-temp path exported to all tests                       | Native library missing after helper success → inspect env/build script and invalidate cache | CI, G1                 | closed for current Linux baseline; full Rust tests passed twice after fresh prepare (`evidence/r0.md`)                                                                                                                                                                      |
| SHP-RSK-012 | Shell artifact publishes to another shell/full-Gosling release or updater channel                                                     | medium     | critical | Profile-specific artifact/feed/repository; publishable bit source-controlled; fixture hard deny; release readback        | Destination/profile mismatch → hard stop before credentials/upload                          | Release, G3/7          | mitigating; fixture publisher/signing denial passes, workflows pending                                                                                                                                                                                                      |
| SHP-RSK-013 | Workflow profile strings/ref/path enable command or path injection                                                                    | medium     | critical | Repository-relative allowlist; canonical parser outputs; avoid `eval`; environment/output quoting; hostile fixtures      | Metacharacter/path escape accepted → block workflow merge                                   | Release security, G3/7 | mitigating; profile path/input controls pass, workflow inputs pending                                                                                                                                                                                                       |
| SHP-RSK-014 | Cross-platform case/normalization/length rules cause IDs or artifact names to collide                                                 | medium     | high     | Platform-aware validators and normalized collision keys; two fixture profiles; target readback                           | Collision on package manager/FS → tighten contract; no manual rename workaround             | Build, G3/7            | mitigating; macOS normalization defect fixed/read back, other OS targets pending                                                                                                                                                                                            |
| SHP-RSK-015 | Fixture is mistaken for a real product or accidentally becomes publishable/updateable                                                 | medium     | high     | Explicit fixture IDs/version/banner/assets; immutable `publishable:false`; workflow deny; no production feed             | Fixture reaches release/upload/sign path → incident/block, delete draft artifact            | Test/release, G3–7     | mitigating; fixture hard denial/negative-space tests pass, workflow proof pending                                                                                                                                                                                           |
| SHP-RSK-016 | Compatibility metadata is incomplete, so a newer shell/core pair proceeds and corrupts or misreads state                              | medium     | high     | Exact bundled-core policy; compare profile/build/runtime/schema/method metadata before session; rollback tests           | Unknown/mismatch → fail closed before session/migration                                     | Runtime, G2/4/6        | mitigating; exact preflight and live zero-session-on-incompatibility pass; upgrade rollback pending                                                                                                                                                                         |
| SHP-RSK-017 | Compatibility checks duplicate/generated DTOs drift from canonical Rust ACP contract                                                  | medium     | high     | Use generated SDK/types; additive Rust metadata only when required; schema regeneration/check                            | Hand-authored duplicate shell DTO found → remove and regenerate                             | Architecture, G2/4     | mitigating; canonical ACP metadata and SDK build used, final schema gate pending                                                                                                                                                                                            |
| SHP-RSK-018 | Broad edits to 3,399-line `main.ts` create unrelated Desktop regressions                                                              | high       | high     | New shell entry/modules; router-only bounded edits; review diff and full Desktop suite                                   | Domain/lifecycle rules added to `main.ts` → stop and extract                                | Architecture, G3–5     | mitigating; focused parser/router owns handoff validation/draft policy, main retains Electron routing only, 688 Desktop tests pass                                                                                                                                          |
| SHP-RSK-019 | Release workflow duplication creates four drifting platform implementations                                                           | high       | high     | Shared resolver/action and reusable adapters; platform-specific code only where necessary                                | Same rule implemented differently in multiple workflows → consolidate before Gate 7 exit    | Release, G7            | open                                                                                                                                                                                                                                                                        |
| SHP-RSK-020 | Signing/notarization/updater testing needs credentials or predecessor artifact unavailable to agent                                   | high       | medium   | Separate human gate; unsigned structural/smoke proof; explicit unverified status                                         | Missing credential/predecessor → do not claim release-ready/published/updater-ready         | Maintainer, G7–8       | open                                                                                                                                                                                                                                                                        |
| SHP-RSK-021 | Package tests become flaky due to polling, process timing, or OS services                                                             | medium     | high     | Observable readiness/events, deterministic temp roots, PID diagnostics; no retry-only fix                                | Repeated pass-after-retry → defect and root-cause work, not green evidence                  | QA, G4/6               | open                                                                                                                                                                                                                                                                        |
| SHP-RSK-022 | Shared configuration is accidentally isolated, breaking intentional credential/profile reuse, or runtime state is accidentally shared | medium     | high     | Explicit path matrix; app identity tests; backend `RuntimePaths` assertions; coexistence sentinel tests                  | Approved shared/isolated path differs → block Gate 6 and correct path owner                 | Runtime/data, G3/6     | open                                                                                                                                                                                                                                                                        |
| SHP-RSK-023 | Deep links or external navigation open arbitrary schemes/hosts or cross product boundary                                              | medium     | critical | Profile-specific scheme; strict URL parse/action allowlist; target ownership tests; full Gosling handoff allowlist       | Unknown action/scheme/host → ignore/diagnose, never forward                                 | Security, G4/6         | mitigating; canonical handoff action is strict and embedded URIs are inert draft data; installed ownership/coexistence pending Gate 6                                                                                                                                       |
| SHP-RSK-024 | Product-specific icons/assets are missing or wrong but build silently falls back to Gosling assets                                    | high       | medium   | Required asset matrix; no fallback for publishable shell; embedded/readback checks                                       | Missing asset → profile check fails before Forge                                            | Packaging, G3/7        | mitigating; macOS arm64 resource readback passes, other targets pending                                                                                                                                                                                                     |
| SHP-RSK-025 | New dependency expands supply chain or cannot run in Electron/CI targets                                                              | low        | medium   | Prefer existing stack/stdlib; dependency rationale/license/vulnerability/replacement cost                                | New dependency proposed → ADR note and all-target validation required                       | Architecture, G2/8     | mitigating; existing transitive asar/plist pinned directly for verifier, all-target validation pending                                                                                                                                                                      |
| SHP-RSK-026 | Domain requirements leak into shared fixture and overfit the host                                                                     | medium     | high     | Neutral fixture, domain slot interface, negative-space audit, two fixture identities                                     | Domain nouns/actions/payload interpretation appear → remove or require separate domain plan | Scope, all             | closed for Gate 3 fixtures; later renderer fixtures still audited                                                                                                                                                                                                           |
| SHP-RSK-027 | Scope expands into publishing real shells or updater promotion without explicit approval                                              | low        | critical | Plan-only authority; fixture non-publishability; human approvals retained                                                | Any upload/sign/publish/update request absent explicit authorization → stop                 | Governance, G7–8       | open                                                                                                                                                                                                                                                                        |
| SHP-RSK-028 | Current main changes significantly before implementation, invalidating paths/contracts                                                | medium     | medium   | Gate 0 reorientation; dedicated worktree; plan-change record; reuse newer extractions                                    | Baseline differs materially → amend file plan/sequence before edits                         | Execution, G0          | open                                                                                                                                                                                                                                                                        |
| SHP-RSK-029 | Test ledgers/docs conventions evolve and new tests become untracked                                                                   | medium     | medium   | Gate 0 inspect current conventions; update any active test inventory/ledger required by repo                             | New ledger appears or AGENTS changes → comply before test commit                            | Governance, all        | open                                                                                                                                                                                                                                                                        |
| SHP-RSK-030 | Signing/update/release identity differs between artifact metadata and OS registration                                                 | medium     | critical | Post-package and installed readback per OS; updater remains disabled until verified                                      | Any mismatch → reject artifact; do not patch metadata post-signing                          | Release, G7–8          | mitigating; macOS fixture bundle-ID mismatch fixed/read back, release/other targets pending                                                                                                                                                                                 |

## Risk acceptance rules

- Critical/high risks involving authority, secrets, process leaks, state collision, artifact cross-publication, workflow injection, unverified downloads, or incompatible state cannot be accepted for a public release.
- A platform/signing risk may remain open only when that target is explicitly not declared release-ready and the handoff names the missing human evidence.
- Every closed risk links to a regression test and final-revision evidence in the traceability matrix or defect ledger.

## Project-shell readiness risks added by the 2026-08-13 reassessment

| ID          | Risk                                                                                                                         | Likelihood | Impact   | Prevention / mitigation                                                                          | Trigger / contingency                                                                          | Owner / gate                 | Status                                                                                                                                                                                                            |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | ------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SHP-RSK-031 | A named shell must edit or copy Gosling renderer/Vite/Forge source because no consumer composition contract exists           | high       | critical | ADR-0010, strict consumer manifest, two independent consumers, third-consumer conformance        | Any product conditional or host edit for a consumer blocks M5                                  | Architecture, R1–R2/R8       | mitigating; ADR-0010 drafted (proposed) 2026-08-13 selecting separate-repository topology with an isolated-workspace fixture retained for conformance; not closed until operator acceptance and R2 implementation |
| SHP-RSK-032 | Widening preload ad hoc to make UI work exposes ACP/process/filesystem authority                                             | high       | critical | Freeze ADR-0011 schemas first; main-owned ACP; exact bounded operations and negative-space tests | Raw ACP URL/token, generic RPC, arbitrary IPC, Node renderer import → stop and redesign        | Security/runtime, R1–R3      | mitigating; ADR-0011 drafted (proposed) 2026-08-13 with a frozen bounded operation table; not closed until operator acceptance and R3 negative-space tests                                                        |
| SHP-RSK-033 | Domain adapter descriptor creates false capability while production runtime has no adapter                                   | high       | critical | ADR-0012 and live capability negotiation; required adapter absence prevents ready                | Descriptor/action differs from live adapter → incompatible/degraded, no action                 | Runtime/domain, R1/R4        | mitigating; ADR-0012 drafted (proposed) 2026-08-13 requiring live capability equality before ready; not closed until operator acceptance and R4 conformance fixture                                               |
| SHP-RSK-034 | Focused shell silently cancels permissions, declines elicitations, or drops updates, making agent workflows fake or unusable | high       | critical | Focused reducers/controllers and explicit UI; no auto-approval; prompt/update integration tests  | Prompt requires interaction or streams output without visible state → block M5                 | Runtime/UI, R3/R5            | mitigated locally after SHP-DEF-039/043/052: callbacks require declared response capabilities, pending records are bounded and structured, and streamed progress cannot clear them; renderer presentation and packaged interaction reproduction remain R5/R6 |
| SHP-RSK-035 | Lifecycle UI presents states that production can never emit or recovery that performs no real action                         | high       | high     | Event-to-state reachability table; remove unreachable states; packaged failure matrix            | Component-only state with no producer/action → fail fake-success audit                         | Runtime/UI, R3–R6            | open                                                                                                                                                                                                              |
| SHP-RSK-036 | Shell package inherits Gosling-specific permissions, metadata, templates, or extra binaries                                  | high       | high     | Exact resource staging; profile/contract metadata; platform permission/readback negative tests   | Unclaimed permission/resource or Gosling identity appears in shell artifact → artifact invalid | Build/security, R2/R6/R7     | open                                                                                                                                                                                                              |
| SHP-RSK-037 | In-process native plugin design couples project code to Gosling ABI and expands supply-chain authority                       | medium     | critical | Prefer versioned out-of-process adapter; explicitly reject arbitrary `cdylib` loading in ADR     | Proposed native library path or domain crate linked into generic CLI → architecture review     | Architecture/security, R1    | mitigating; ADR-0012 drafted (proposed) 2026-08-13 explicitly rejects cdylib/native-library loading in favor of an out-of-process adapter; not closed until operator acceptance                                   |
| SHP-RSK-038 | Local macOS success hides current Linux helper failure and status rounds partial evidence up to green                        | realized   | high     | R0 repair plus two clean Linux runs; revision-bound status                                       | Any required red check blocks baseline/project-shell-ready claim                               | CI, R0/R8                    | closed for R0; runs `31731952749` and `31732990062` passed Linux Rust after the original failure                                                                                                                  |
| SHP-RSK-039 | Separate project repos cannot consume source-bound profiles/scripts and therefore fork the host                              | high       | high     | Decide distribution topology; external/isolated consumer conformance and version policy          | Consumer requires Gosling checkout edits or unpublished internals → block M5                   | Architecture/build, R1/R2/R8 | mitigating; ADR-0010 drafted (proposed) 2026-08-13 selects separate-repository consumer topology; not closed until operator acceptance and R2/R8 external-consumer conformance                                    |

## Default Shell DS-3–DS-7 risks added by the 2026-08-14 implementation plan

| ID          | Risk                                                                                                                            | Likelihood | Impact   | Prevention / mitigation                                                                                                                                              | Trigger / contingency                                                                                                                 | Owner / gate                   | Status                                                                                                                                                                                                                               |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| SHP-RSK-040 | A chosen directory is accepted from renderer input, changes after early validation, or mutates global Gosling workspace state   | medium     | critical | Native main-owned chooser; Rust canonical validation at selection and session creation; side-effect and TOCTOU tests                                                 | Caller path reaches IPC, canonical paths disagree, or workspace/config changes → block DS-3                                           | Filesystem/runtime, DS-3       | mitigated on `259935f01`; native/main ownership, double canonicalization, TOCTOU, live session-directory, clean-package, and exact-source CI evidence pass                                                                             |
| SHP-RSK-041 | A shell-safe credential list leaks secrets or broad profile metadata by filtering the existing workspace DTO too late           | medium     | critical | Rust-owned minimal projection; sentinel-field tests across ACP, SDK, main, preload, settings, diagnostics, and package                                               | Any excluded field/value crosses the shell response → disable selectable mode and block DS-4                                          | Security/runtime, DS-4         | mitigated locally; the four-field Rust projection and sentinel negative-space tests pass, and packaged unavailable-state replay exposes no credential material                                                                       |
| SHP-RSK-042 | Credential selection silently widens provisioning authority or copies credentials into shell-local storage                      | medium     | critical | Explicit fixed/selectable policy; opaque ID only; backend re-resolution and pinning; no shell credential mutations                                                   | Unprovisioned profile is selectable, secret material is persisted, or revocation substitutes a profile → block DS-4                   | Security/config, DS-4          | mitigated on `259935f01`; unknown IDs fail closed, configured profiles are re-resolved and pinned by ID/name, and exact-source CI passes                                                                                               |
| SHP-RSK-043 | A module registry exposes installed-but-undeclared backends or becomes a generic renderer-to-backend invocation bus             | medium     | critical | Intersection of provisioning/live/consumer/protocol authority; bounded summaries; retain typed routes and one-adapter v1                                             | `module.call`, endpoint/transport exposure, or union-based discovery appears → block DS-5                                             | Architecture/security, DS-5    | mitigated locally; intersection, bounds, typed-route negative space, failure matrix, and packaged `core:session` projection pass                                                                                                     |
| SHP-RSK-044 | Scaffold generation overwrites user work or emits a publishable, credential-bearing, developer-enabled, or domain-shaped shell  | medium     | critical | Approved roots; prevalidation; refuse existing destinations; staged atomic rename; strict neutral conformance                                                        | Existing hash changes, partial output remains, or unsafe field/default is emitted → block DS-6                                        | Build/governance, DS-6         | mitigated on `259935f01`; hostile scaffold/conformance, second-neutral-identity, clean-package, and exact-source CI evidence pass                                                                                                     |
| SHP-RSK-045 | Dirty-tree or historical CI evidence is treated as acceptance for later Default Shell work                                      | high       | high     | One isolated exact revision; regenerated-artifact idempotence; current mandatory CI; explicit operator GUI decision                                                  | `sourceClean:true`, exact SHA, current CI, or an exit condition is absent → automatic DS-7 NO-GO                                      | Governance/QA, DS-7            | mitigated for technical evidence on `259935f01`/merged tree `240ab7515`: clean-source package/readback and current CI pass; explicit operator acceptance remains the final gate condition                                                |
| SHP-RSK-046 | Synchronous protected-credential discovery stalls shell startup or session creation while the async server cannot make progress | medium     | high     | Skip catalog access when unnecessary; run required discovery on a blocking worker with a fail-closed deadline; expose bounded preflight phases                       | A packaged shell remains in `validating`, credential refresh never returns, or an unknown profile reaches a session → block DS-4/DS-7 | Runtime/security, DS-4/DS-7    | mitigated on `259935f01` by SHP-DEF-036/037 regressions, real packaged readiness, clean-package readback, and exact-source CI                                                                                                         |
| SHP-RSK-047 | A known session ID bypasses the current-directory picker and loads history or tools under another working directory             | medium     | critical | Main supplies the accepted canonical directory to resume; compare server session metadata before `loadSession`; safe failure projection and live mismatch regression | Any mismatched directory reaches `loadSession` or becomes active → block DS-3/DS-7                                                    | Filesystem/runtime, DS-3/DS-7  | mitigated on `259935f01` after SHP-DEF-048: unit, live managed-backend, packaged-preload, clean-package, and exact-source CI evidence prove fail-closed directory binding                                                           |
| SHP-RSK-048 | A non-Electron ACP caller relocates a shell session or resumes authority that current shell provisioning removed                | medium     | critical | Enforce the stored canonical directory and current provisioning in the Rust activation path; replace persisted extension/skill selections before activation          | A shell load accepts a different directory or exposes a tool omitted by current provisioning → block DS-1/DS-3/DS-7                   | Backend policy, DS-1/DS-3/DS-7 | mitigated on `259935f01` after SHP-DEF-049: shell-runtime E2E and exact-source CI prove exact directory binding, stale developer-tool removal, and obsolete skill-selection clearing                                                 |
| SHP-RSK-049 | Repository tests read or block on the operator's real protected credential store, making acceptance unsafe and non-reproducible | medium     | high     | Force unit-test `Config` instances to file-backed secret storage; retain a feature-enabled regression; run the full workspace without a disable override             | Any unit test enters the system keyring or requires operator credentials → block DS-7                                                 | Test/security, DS-7            | mitigated on `259935f01` after SHP-DEF-050: exact-final isolated workspace and exact-source CI pass with system-keyring support and no disable override                                                                               |
| SHP-RSK-050 | A timed-out protected-store lookup survives in a blocking worker and prevents normal managed-backend shutdown                   | medium     | high     | Managed backends disable interactive protected-store prompts; wait briefly for ACP close before signalling the child; retain forced cleanup and process registry     | Normal packaged stop reaches `SIGKILL`, a credential lookup survives timeout, or registry/process state remains → block DS-4/DS-7     | Runtime/security, DS-4/DS-7    | mitigated on `259935f01` after SHP-DEF-051: packaged readiness/normal shutdown, empty-registry, clean-package, and exact-source CI evidence pass                                                                                       |
