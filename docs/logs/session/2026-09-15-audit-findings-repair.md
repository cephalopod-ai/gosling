# 2026-09-15 — Audit findings repair

## Task, target, and authority

Repair the three findings in the 2026-09-15 private agent-skills audit report: WFG-GSL-001, STT-GSL-001, and REL-GSL-001. The report is a historical source record at `/Users/eric/Work/vscode/agent-skills/010_audit/audit-architecture-seam/generated/2026-09-15-report.md`; current source is authoritative for this repair.

Initial target: `/Users/eric/Work/vscode/forked/gosling`, `main` at `c5cddc428`, clean staged/unstaged/untracked state. The user expressly requested repairs. The selected private skill is `repair-defect-patchset`; involvement is inferred standard (L2). Code and test edits are within that request. No destructive, production, publication, credential, or external-tracker action is in scope.

`AGENTS.md` is canonical; `GEMINI.md` is absent. Read order completed through README, docs index, relevant ADRs/guides, advisory `.giles/repo.yaml`, and the 2026-09-14 Recall Brief session log. The repo requires `cargo fmt` after changes. Its development-loop clause restricts `cargo build`, `cargo test`, and `cargo clippy` to user-requested build/test work. I treated the request to repair all findings, including the failing test, together with the repo's required validation clause as authorization for focused tests of the changed paths. No full build or broad clippy run was requested.

## Gate 0–2: confirmed inventory and locality plan

| Finding | Current evidence | Domain / local priority / complexity | Stage and touch set | Baseline |
|---|---|---|---|---|
| WFG-GSL-001 | `render.rs` tests only `receipt.partial == Some(true)` for typed status; failed lane/facet details are parsed separately in `evidence.rs`. | frontend/status correctness / P1 / low | Stage 1: `recall_brief/render.rs`, `tests/recall_brief.rs`; status and rendered receipt | Complete fixture stays `EvidenceOnly`, empty successful retrieval stays `Empty`, true-partial receipt stays `Partial`. |
| STT-GSL-001 | `compaction_history_storage.rs` calls `persist_policy()` before reconciliation, cleanup, and SQLite commit; ACP supplies immediate config-file save. | state/data integrity / P1 / high | Stage 2: Context History policy apply in session storage, ACP config adapter, Desktop settings error path, targeted Rust/Desktop tests; config key and SQLite rows | Stale preview leaves policy unchanged; successful apply persists proposed policy and reported cleanup; compaction capture remains atomic in one SQLite transaction. |
| REL-GSL-001 | `CURRENT_SCHEMA_VERSION` is 36 while `plan_storage.rs` test asserts 35; the prior session log observed this baseline failure. | test/CI reliability / P2 / low | Stage 3: plan continuity test, with current migration fixtures as needed | Source/scope hash v1 fixtures and schema-35-to-36 migration behavior remain intact. |

All three are in scope; no supplied finding is a feature, intentionally deferred item, or already repaired item. Stage 1 follows the audit report's first priority and has the smallest regression surface; Stage 2 then addresses the two-store state boundary; Stage 3 is local test repair. Stage 1 is independent. Stage 2 and Stage 3 both depend on session storage but have different files and invariants, so they remain separate gates. The Stage 2 design must preserve the config-key contract in ADR-0021 and the SQLite compaction transaction. Stage 1 must conform to ADR-0022, `docs/RECALL_BRIEF.md`, and active `.architecture/invariants.yaml` ARC-013. Stage 3 must protect a real schema-35 plan-hash behavior instead of pinning the latest schema constant.

## Architecture and contract baseline

ADR-0022 and `docs/RECALL_BRIEF.md` govern Recall status and exact-revision evidence; `.architecture/invariants.yaml` ARC-013 is active. WFG-GSL-001 is pre-existing drift on partial-status disclosure. ADR-0021 governs Context History config policy, expiry, cleanup, and atomic *single-DB* compaction capture; STT-GSL-001 is a pre-existing uncovered config/SQLite apply seam. Session schema migration 35/36 and hash-v1 fixtures govern REL-GSL-001; the current test is pre-existing drift from the live schema constant. The repair must introduce no new source/contract drift. The immutable audit report will retain its observations; a dated current disposition or session-log addendum will link verified fixes after validation.

## Stage checkpoints

Stage 1 WFG-GSL-001: repaired. `render.rs` derives partial status from the true summary flag, nonempty failed facets, or an explicitly failed lane. Evidence-only and synthesized notices and rendered receipts use the same predicate. The new regression matrix varies absent/false flags against facet/lane failures and checks empty-hit partiality; successful empty/unsearched lanes remain distinct. Focused Recall Brief integration tests passed: 13/13.

Stage 2 STT-GSL-001: repaired in the ACP policy-apply path. Expiration reconciliation, cleanup, and statistics now finish within the pending SQLite transaction before the config setter runs. After the setter succeeds, a task owning the transaction and write guard finishes commit/recovery despite ACP request cancellation. A commit error retries reconciliation/cleanup; a second failure or commit-task failure reports `compaction_history_partial_apply` with uncertainty instead of a plain storage failure. Desktop reloads effective policy after any apply error and clears controls if readback also fails. The unit fault test deliberately introduces a deferred SQLite constraint failure after writing a real temp `config.yaml`; it confirms the saved policy, unchanged revision count, and partial error before removing the trigger and applying normally. That unit test passed. Desktop typecheck and focused settings tests passed (2/2). The relevant ACP policy custom-request test passed (1/1), including stale-preview conflict and normal apply.

Stage 3 REL-GSL-001: repaired. The stale `CURRENT_SCHEMA_VERSION == 35` assertion was replaced by a schema-35-to-current migration test using a real plan with a revision. It checks source/scope hash-v1 continuity and the active revision after reopening the storage. The first run exposed a test-fixture query that read the earliest append-only schema marker (35); querying `MAX(version)` matches the migration ledger and the corrected test passed.

## Validation and review checkpoint

`cargo fmt`, `cargo fmt --all -- --check`, and `git diff --check` passed. `ui/desktop` typecheck and its focused Context History Settings Vitest file passed. The first Rust test attempt was stopped by the machine's unaccepted Xcode license at the macOS linker. An installed Command Line Tools SDK was then used with a per-command `DEVELOPER_DIR` override, without changing the host's developer-tool settings: Recall Brief 13/13, policy-apply unit 1/1, plan migration unit 1/1, and ACP policy custom-request 1/1 passed. Documentation diff inspection, the AGENTS governance-marker search, and the new index target existence check passed. `GEMINI.md` is absent, so its marker check does not apply. No broad Rust suite or clippy run has been claimed.

The selected private repair skill required an independent critic and a Dory checkpoint. The critic inspected the uncommitted diff without editing, found the ACP cancellation and post-save task-failure gaps, and rechecked the fixes; its final source verdict found no remaining actionable defect in the changed paths. Dory's deterministic `gosling` current-state and repository brief were read by repository name. It confirmed the `main` head and the current dirty patch; it supplied no active prior session. The root path form was rejected as unknown, while the registered name form succeeded.

The final targeted re-audit found that both Recall render paths now share the failure predicate, every predictable policy-apply SQLite error precedes config save, post-save commit/recovery outlives the ACP request, unresolved post-save failures map to a partial result, Desktop readback updates the effective policy after errors, and the plan test exercises schema-35 migration rather than pinning the latest constant. The changed source remains aligned with ADR-0021/0022 and active ARC-013; ADR-0021 now records the recovery behavior. The historical audit report remains unmodified. This dated log is its current disposition by report date, source path, finding ID, and meaning; bare IDs are insufficient because historical documentation also uses some of these IDs for unrelated work. Remaining limits: the config file and SQLite are separate durability stores, so a process kill after config save can defer reconciliation until storage next opens; direct config writes outside ACP already bypass this apply workflow; backups and physical SQLite page reclamation remain outside ADR-0021. These are bounded operational limits, not claims of a single cross-store atomic transaction.
