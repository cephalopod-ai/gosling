# 2026-08-13 Gosling shell R1 ADR-drafting checkpoint

- Task: implement `pre-gui-backend-implementation-plan.md`. R0 was already complete on `main` at `3feffca7c`; current gate was R1 ("freeze project-shell topology and application contracts"), whose deliverables are architecture only — ADR-0010–0012 and frozen schemas — not runtime code.
- Read order followed: `AGENTS.md`, `docs/INDEX.md`, `pre-gui-backend-implementation-plan.md`, `project-shell-readiness-plan.md`, `readiness-reassessment.md`, `build-state.md`, `traceability-matrix.md`, `risk-register.md`, `assumption-ledger.md`, `defects.md`, `plan-changes.md`, `docs/architecture/shell-productization-contracts.md`, `docs/architecture/shell-foundation.md`.
- Source grounding: confirmed exact current-state facts before drafting — `ui/desktop/shell.html`/`vite.shell.renderer.config.mts`/`forge.config.ts` hard-code one renderer; `GoslingShellAPI` exposes exactly 8 channels (`ipc.ts`); `ShellAcpConnection`/`clientCallbacks()` in `acpRuntime.ts` cancel permissions, decline elicitations, and discard session updates; `DomainAdapter` (`crates/gosling/src/acp/shell.rs`) has no production caller — `build_shell_runtime` in `crates/gosling-cli/src/cli.rs:1359` always passes `None`; `ResolvedShellProductProfile`/`ShellBuildManifest` (`profile.ts`) have no renderer-entry or capability field.
- Implementation (documentation only, no runtime/IPC/Rust code changed):
  - `docs/adr/0010-project-shell-consumer-composition.md` — selects separate-repository consumer topology, in-tree isolated-workspace fixture retained for conformance.
  - `docs/adr/0011-shell-application-runtime-boundary.md` — main-owned bounded session/prompt/cancel, explicit action-bound permission/elicitation mediation, safe runtime snapshot v2.
  - `docs/adr/0012-shell-domain-adapter-topology.md` — versioned out-of-process adapter, server-owned authorization, action-bound confirmation tokens; rejects in-process/`cdylib` loading.
  - `docs/architecture/shell-productization-r1-contracts.md` — companion schema freeze (consumer manifest v1, snapshot v2, application-runtime operation table, interaction records, adapter descriptor/confirmation token, error taxonomy additions, negative-space rules).
  - All three ADRs and the addendum are marked **Status: proposed — pending operator acceptance**, not "accepted for implementation" (the pattern used by ADR-0007–0009), because the parent plan names the underlying topology/transport choices as open human decisions (`assumption-ledger.md` SHP-ASM-029, SHP-ASM-031) and states "Draft ADRs are not R1 completion."
- Tracking updates: `traceability-matrix.md` (SHP-REQ-033–038/042 rows and the R1 forward-trace row), `risk-register.md` (SHP-RSK-031/032/033/037/039 → `mitigating`, still open pending acceptance), `assumption-ledger.md` (SHP-ASM-029/031 note the draft direction, still `unresolved`), `defects.md` (SHP-DEF-021/022/023/024/026/029 note the ADR reference, still `open`), `plan-changes.md` (SHP-PC-004, level L2), `docs/INDEX.md`, `docs/build/shell-productization/README.md`, `build-state.md`.
- Explicitly not done, and why: no Vite/Forge/IPC/preload/Rust code changed. The plan's dependency-aware sequence and PG-INV rules forbid implementing R2 (consumer resolver), R3 (session/prompt IPC), or R4 (adapter registration) before R1's ADRs are architecture-reviewed and accepted — that acceptance is an operator decision this session cannot self-grant. `build-state.md` states this explicitly so the gate is not rounded up to complete.
- Validation run: `git diff -- AGENTS.md GEMINI.md docs README.md` (no runtime files touched); `grep -R "GILES:DOCS-GOVERNANCE:START" -n AGENTS.md` (present, unmodified); no code/tests changed, so `cargo fmt`/`cargo test`/`pnpm` suites were not run — this is a documentation-only change touching zero `.rs`/`.ts`/`.tsx` files.
- Next action: obtain operator/architecture review of ADR-0010–0012; only after acceptance may R2 (PG-20) begin.
