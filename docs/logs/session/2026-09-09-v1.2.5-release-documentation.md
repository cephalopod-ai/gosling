# v1.2.5 release documentation preparation

- Date: 2026-09-09
- Task: prepare the next local source version and update release/user documentation for the major
  changes after v1.2.4 without tagging or publishing an unvalidated release.
- Target: `/Users/eric/Work/vscode/forked/gosling`, branch `main`, clean baseline `800a76879`.
- Workflow: searched the private agent-skills catalog and used its GitHub release governance
  contract for local preparation, source readback, validation, and publication boundaries.

## Release identity

- Selected `1.2.5` under the repository's single-digit patch increment policy.
- Confirmed that `v1.2.4` is the ancestor of the eight candidate commits. The unrelated inherited
  `v2.0.0-rc-*` tags are on another history and do not change this release line.
- Confirmed through GitHub readback that the latest release is named `v1.2.4` but is attached to
  `release_v1.2.4`. The separate annotated `v1.2.4` tag resolves to the same source commit and has
  no release object. Neither historical tag was moved.
- Confirmed the candidate range contains 234 changed files, 11,004 insertions, and 18,750
  deletions. The release notes distinguish deliberate duplicate-code removal from feature loss.

## Files changed

- Bumped `Cargo.toml`, the nine gosling workspace entries in `Cargo.lock`, and
  `ui/desktop/package.json` from 1.2.4 to 1.2.5 with `just bump-version 1.2.5`.
- Repaired the release recipe in `Justfile`: it no longer requires, mutates, or stages the deleted
  `ui/desktop/openapi.json` file.
- Added `documentation/docs/release-notes/v1.2.5.md` and updated `README.md`, `RELEASE.md`,
  `RELEASE_CHECKLIST.md`, documentation indexes, and the documentation inventory.
- Documented Full Session Handoff continuity, the selectable Safe-by-default crash policy,
  multiline prompt submission, output-preview resolution, and the ACP-only architecture.
- Removed active documentation for the deleted direct `codex`, Amp ACP, Avian, and Gemini OAuth
  provider implementations. Replaced stale `goslingd` and REST setup with the current
  authenticated `gosling serve` ACP flow.
- Added the provider-managed compaction countdown regression to user-facing known issues,
  `docs/TODO.md`, the candidate notes, and the release checklist.

## Validation

- `just bump-version 1.2.5`: passed after repairing the removed OpenAPI version surface.
- `just verify-version-surface-paths` and `just get-tag-version`: passed; reported `1.2.5`.
- `cargo metadata --locked --no-deps --format-version 1`: passed; all nine gosling workspace
  packages report `1.2.5` and only `gosling-fuzz` retains its independent `0.0.0` version.
- `cargo fmt --all -- --check`: passed.
- `npm test` in `documentation/`: passed, 16 tests.
- `npm run typecheck` in `documentation/`: passed.
- `npm run build` in `documentation/`: passed and exported 172 Markdown pages. The ignored
  generated docs map, skills manifest, and skill archives were rebuilt successfully.
- `git diff --check` and the required AGENTS governance-marker check: passed before this log was
  added; the final diff check was rerun after all documentation edits.

## Risks and open gates

- `CMP-OWN-001` remains open: provider-managed sessions can receive a negative or oscillating
  gosling compaction countdown even though the connected provider owns compaction. Clamping to zero
  is not an adequate repair; publication remains blocked on ownership-aware suppression and tests.
- Candidate-wide Rust tests, Clippy, Desktop tests/typecheck, packaged artifacts, signing,
  notarization, updater, installed-app, and clean-machine checks were not run in this preparation.
- No commit, tag, push, GitHub release, documentation deployment, or updater promotion was
  performed.
